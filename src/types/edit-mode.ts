/**
 * Edit mode enumeration
 *
 * Defines the different editing modes for the S3 explorer UI
 */

export enum EditMode {
  Normal = 'normal',
  Visual = 'visual',
  Insert = 'insert',
  Edit = 'edit',
  Search = 'search',
  Command = 'command',
}
